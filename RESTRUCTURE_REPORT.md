# Restructure report: Phase A of REBUILD_BRIEF.md

Written at the Phase A gate, 2026-09-17. Everything below is a result; the
pre-brief HEAD is `fa2aaa8`, and the gate commit is the one whose message
begins `gate:`.

## 1. What changed

- `src/pages/` mirrors `docs/`: 32 pages under 3,000 words of prose are single
  files copied verbatim; 11 pages of 3,000 words or over are directories of
  fragments, 112 fragment files in all, cut at line boundaries with
  `000-head.html`, one `NNN-<h2 id>.html` per top-level section and
  `999-foot.html`. Produced by a one-off splitter, `tools/split-pages.mjs`,
  committed in the first commit of this run and deleted in the gate commit.
- `tools/build.mjs` writes `docs/` from `src/pages/` by verbatim copy or
  verbatim concatenation, and `--check` fails in `file:line message` form on
  any page that differs, any source without a page, any page without a source,
  and any manifest entry without a source. The generated-page list is empty in
  this phase; the two depmap views are treated as foreign, written and held
  fresh by `tools/depmap.mjs`.
- `tools/verify.mjs` gains section 9, which runs the build's check. Sections 1
  to 8 are untouched.
- `docs/decisions/dr-028-fragments-and-build.html` records the decision; DR-010
  is revised in DR-017's form (stamp, Revised row, a status paragraph beginning
  "Revised 2026-09-17:"); the ledger gains a row; `manifest.json` gains the
  entry, the decisions count and the dates; `tools/depmap/graph.json` gains
  `dr-028`, three `alt-dr-028-*` nodes, `rejects` edges to them and
  `justifies` edges from `dr-010` and `ledger-1.11.3`. `refs.json` was
  regenerated by `extract`, and both served views by `view --site`.
- `CLAUDE.md` at the root (65 lines), `models/CLAUDE.md` (25 lines) and
  `tools/depmap/CLAUDE.md` (13 lines).
- `.claude/commands/impact.md`, `section.md`, `verify.md`.
- Every file a model script writes moved to `models/out/`, with every writer,
  reader and figure script updated; see section 6.
- `README.md`: the table and an Editing section for the build; nothing else.

Not edited: `docs/assets/`, `netlify.toml`, the logic of `models/*.py`,
sections 1 to 8 of `tools/verify.mjs`, `tools/depmap.mjs`,
`SURFACING_REPORT.md`, and the prose and markup of every page.

## 2. The docs diff against the pre-brief HEAD (gate item 3.2)

```
$ git diff --name-only fa2aaa8 -- docs/
docs/decisions/dr-010-html-no-build-step.html
docs/decisions/dr-028-fragments-and-build.html
docs/decisions/index.html
docs/depmap/architecture.html
docs/depmap/index.html
```

One deviation from the list the brief names: `docs/depmap/architecture.html`
was regenerated as well as `depmap/index.html`. Both served views embed the
page list from `refs.json`, so adding DR-028 changed both, and section 8 of the
harness fails on a stale view. Every other file under `docs/` is
byte-identical to the pre-brief HEAD. In the step 2 commit, before DR-028 was
added, `git diff --stat -- docs/` was empty after the first build.

The changes to `dr-010`, `index.html` under `decisions/` and the ledger are the
additions the brief names: DR-010's stamp, Revised row, status paragraph and
footer date; the ledger's new row and its revised dates (stamp, meta, footer),
which is the form its neighbours took when DR-027 was added.

## 3. The checks at the gate

```
$ node tools/build.mjs --check
build --check: clean. 43 pages.
$ node tools/verify.mjs
verify: clean. 43 pages, 3 stylesheets, 6/6 mandated figures.
$ node tools/depmap.mjs check
check: clean. 409 nodes, 641 edges, 1 warning(s) (--warnings).
```

The one warning predates this run (a closedness scan on a vocabulary, printed
only with `--warnings`). The freshness check was proved negative before it
was committed: a one-word edit to line 300 of the assembled 02 and a stray
`docs/stray.html` produced

```
docs/02-architecture/index.html:300: differs from its source src/pages/02-architecture/index/; edit the source and run node tools/build.mjs
docs/stray.html:1: page has no source under src/pages/ and is not a generated page; add src/pages/stray.html or remove it
```

from both `build --check` and `verify`, and the build restored the page.

## 4. Word counts before and after

Counted by `words()` in `tools/build.mjs`: tags, `<script>`, `<style>` and
inline `<svg>` stripped, entities decoded, whitespace-separated tokens. Pages
under 3,000 are copied verbatim and keep their count. The head and foot
fragments carry the masthead, rail, page header and footer.

| Page | Words before | Fragments (words) |
|------|-------------:|-------------------|
| `index.html` | 2591 | copied verbatim |
| `00-thesis/index.html` | 7475 | 000-head 168; 001-customer-and-wedge 857; 002-context-profile 252; 003-why-now 1310; 004-markets 675; 005-landscape 1740; 006-moat 778; 007-endgame 526; 008-sources 1040; 999-foot 129 |
| `01-product/index.html` | 11004 | 000-head 134; 001-journeys 3279 (over); 002-taxonomy 854; 003-intake 1020; 004-envelope 1787; 005-verdicts 891; 006-onboarding 1307; 007-refusals 1366; 008-sources 237; 999-foot 129 |
| `02-architecture/index.html` | 22700 | 000-head 125; 001-overview 791; 002-planner 1424; 003-classifier 4099 (over); 004-surfaces 4738 (over); 005-state-machine 3676 (over); 006-adapters 999; 007-verification 2206; 008-evidence-pipeline 1449; 009-context-store 1293; 010-intake-gateway 1771; 999-foot 129 |
| `02-architecture/worker-console.html` | 5624 | 000-head 144; 001-task-card 1222; 002-alias-only 1225; 003-outcome-codes 864; 004-dual-control 1055; 005-exceptions-board 760; 006-invariant 225; 999-foot 129 |
| `03-trust-and-data/index.html` | 24102 | 000-head 161; 001-alias-model 4413 (over); 002-substitution 3062 (over); 003-surfaces 4029 (over); 004-redaction 1259; 005-passports 2642; 006-policy-flags 1723; 007-threat-model 3777 (over); 008-access-ledger 1249; 009-residency 498; 010-privacy-law 581; 011-sources 579; 999-foot 129 |
| `03-trust-and-data/trust-story.html` | 1401 | copied verbatim |
| `04-operations/index.html` | 8802 | 000-head 148; 001-task-cards 2752; 002-shifts 1007; 003-phone-scope 2448; 004-site-controls 1066; 005-qa 574; 006-slas 608; 007-sources 70; 999-foot 129 |
| `05-business/index.html` | 7283 | 000-head 145; 001-why-this-page 173; 002-task-model 153; 003-the-ratio 290; 004-cost-split 564; 005-trajectory 829; 006-how-users-arrive 2174; 007-funding 564; 008-geography 1377; 009-commerce 514; 010-what-replaces 371; 999-foot 129 |
| `05-business/measurement-protocol.html` | 2025 | copied verbatim |
| `05-business/model.html` | 2874 | copied verbatim |
| `06-roadmap/index.html` | 5963 | 000-head 141; 001-sequence 1939; 002-pilot 986; 003-build-buy 1471; 004-expansion 1119; 005-sources 178; 999-foot 129 |
| `07-open/index.html` | 5173 | 000-head 127; 001-items 4428 (over); 002-objections 370; 003-left-alone 119; 999-foot 129 |
| `08-review/index.html` | 25850 | 000-head 472; 001-executive-verdict 1052; 002-the-idea-in-one-page 589; 003-technology-today 1713; 004-horizon-threats 1087; 005-regulation-india 1928; 006-regulation-uk 1317; 007-regulation-us 1537; 008-market-and-competition 1752; 009-economics 1561; 010-team-and-execution 1519; 011-security-red-team 1608; 012-product-and-gtm 1591; 013-pre-mortem 1329; 014-what-we-would-need-to-see 875; 015-questions-for-the-founder 901; 016-what-the-record-should-change 1256; 017-sources 3634 (over); 999-foot 129 |
| `decisions/dr-004-authority-envelope.html` | 3092 | 000-head 131; 001-decision 2223; 002-rejected 208; 003-closes-off 258; 004-record-status 62; 005-sources 81; 999-foot 129 |
| the other 26 decision records and `decisions/index.html` | 598 to 2190 | copied verbatim |

Two shapes worth knowing. In `04-operations`, one `<section>` carries two
`h2` headings, `phone-scope` and `tiers`; the fragment is named for the first
and holds both, because the markup was not changed. In `dr-004`, the four
record sections sit inside `<article class="dr">` and the sources section sits
after it, so `004-record-status.html` ends with the article's closing tag and
`005-sources.html` follows; the concatenation is byte-identical, as it is for
every page. `dr-028`, added after the split at 1,187 words, is a single file.

## 5. Exceptions under gate item 3.3

Ten fragments are single top-level sections over 3,000 words. Each is one
`<section>`, allowed by the brief and listed here:

| Fragment | Words |
|----------|------:|
| `src/pages/02-architecture/index/004-surfaces.html` | 4738 |
| `src/pages/03-trust-and-data/index/001-alias-model.html` | 4413 |
| `src/pages/07-open/index/001-items.html` | 4428 |
| `src/pages/02-architecture/index/003-classifier.html` | 4099 |
| `src/pages/03-trust-and-data/index/003-surfaces.html` | 4029 |
| `src/pages/03-trust-and-data/index/007-threat-model.html` | 3777 |
| `src/pages/02-architecture/index/005-state-machine.html` | 3676 |
| `src/pages/08-review/index/017-sources.html` | 3634 |
| `src/pages/01-product/index/001-journeys.html` | 3279 |
| `src/pages/03-trust-and-data/index/002-substitution.html` | 3062 |

The largest file a session now opens is 4,738 words, against 25,850 before.

## 6. models/out (deliverable 2.5)

Every file a script writes was found by reading the scripts: 42 files, all the
`.csv` and `.json` under `models/`, moved with `git mv` to `models/out/`. The
`.md` files are notes and the two `.xlsx` files are kept by hand; no script
writes them, so they stay where they were. `models/README.md` said the
probabilistic workbook was "output of aifred_model.py"; the script writes no
workbook, and the row now says it is kept by hand from that output.

Path changes, and nothing else in the scripts: `aifred_model.py` and
`aifred_geo_model.py` wrote to the working directory and now write beside
themselves under `out/`, from an `OUT` defined at the tail of each file, after
the markers the harness slices at, so the exec'd pieces are unchanged;
`aifred_harness.py` exports `OUT` beside `HERE`; the four scripts built on it
read and write under `OUT`; `aifred_geo_growth.py`'s `MARK_WRITE` now names
the `OUT` line so its slice of the geography file ends where it did;
`tools/figures/forecast-05.py` and `growth-05.py` read `models/out/`.

Each script run once, from `models/`, with Python 3.11.15, numpy 2.4.6 and
pandas 3.0.5, in this order:

| Script | Result | Time |
|--------|--------|-----:|
| `aifred_model.py as_specified 20000 20260913` | ok | 1 s |
| `aifred_model.py viable 20000 20260913` | ok | 2 s |
| `aifred_geo_model.py` for `india_only`, `expansion`, `expansion_commerce`, `foreign_led` | ok | 2 s each |
| `aifred_growth_model.py --selftest`, then the full run | ok, rebuilds `sim_viable.csv` exactly | 2 s, 16 s |
| `aifred_structural_probes.py` | ok | 12 s |
| `aifred_geo_growth.py --selftest`, then the full run | ok, all four geography runs rebuilt exactly | 6 s, 47 s |
| `aifred_sensitivity.py` | ok, rebuilds `sim_viable.csv` exactly | 56 s |
| `tools/figures/forecast-05.py`, `growth-05.py` | ok; the five figure blocks written are identical to the five in `docs/05-business/index.html` | under 1 s |

Every output that was committed regenerated byte for byte: after the runs,
`git status models/out` showed no modified file. Six files the scripts write
had never been committed and now are: `summary_as_specified.json`,
`summary_viable.json` (from `aifred_model.py`) and `geosum_india_only.json`,
`geosum_expansion.json`, `geosum_expansion_commerce.json`,
`geosum_foreign_led.json` (from `aifred_geo_model.py`).

One file has no writer: `models/out/sim_growth.csv`, the line the publication
quotes and the input of both figure scripts, is read by the scripts and
written by none of them. `aifred_growth_model.py` writes
`sim_growth_india_only.csv` and `sim_growth_net_of_tax.csv` beside it but
not the file itself. It cannot be regenerated from the repository as it
stands.

On gitignoring `models/out/`: every committed output regenerates cleanly in
under three minutes, which argues for ignoring; against it, `sim_growth.csv`
has no writer, and every self test compares a fresh run against the committed
CSV, so the committed files are the evidence the self tests need. Keeping
them committed is the safer default until `sim_growth.csv` has a writer. The
founder decides.

## 7. Prose the founder should update (not edited, by constraint 1.4)

Adding DR-028 without touching prose leaves four statements stale. None is
checked by the harness or the map, and none was edited.

- `docs/index.html`, section 7: "The decision ledger lists twenty-seven
  records." There are now twenty-eight.
- `docs/decisions/index.html`, standfirst: "Twenty-seven records hold every
  decision"; "Twenty-two are fixed". Now twenty-eight and twenty-three.
- `docs/decisions/index.html`, section 1: the paragraph on when each record
  was taken ends at DR-027.
- DR-010, 1.1 and 1.2: "The only tool in the publishing path is
  tools/verify.mjs, which reads and never writes" and "the page count is small
  enough that the discipline is cheaper than the machinery". Both are amended
  by the status paragraph, as the brief prescribes, and the decision text is
  left as it was.

## 8. Commits of this phase

1. `build: assemble docs/ from src/pages/ fragments, byte for byte`
   (`REBUILD_BRIEF.md`, `tools/build.mjs`, the splitter, `src/pages/`).
2. `verify: section 9 holds docs/ to the build's freshness check`.
3. `DR-028: pages assembled from fragments, held fresh by the harness; DR-010
   revised` (the record, the revision, the ledger, the manifest, the graph,
   `refs.json`, both views).
4. `CLAUDE.md, commands, models/out and the README for the build`.
5. The gate commit: the splitter deleted, the six new outputs, this report.

## 9. A question to test CLAUDE.md

Ask a fresh session: "What must I do before editing the classifier section of
architecture?"

The answer it should give: read the summary of `02-architecture/index.html`
in `manifest.json`; the classifier is a mechanism stated on more than one page
and carries two closed lists, so run `node tools/depmap.mjs impact
comp-classifier` (or `/impact comp-classifier`), read REOPENS first, since a
ledger item or a record there makes the change the founder's call, and save
the closure under `tools/depmap/batches/NNNN-<slug>.md`; then open only
`src/pages/02-architecture/index/003-classifier.html`, never
`docs/02-architecture/index.html`; after the edit run `node tools/build.mjs`,
`node tools/verify.mjs`, and `node tools/depmap.mjs extract` then `check`,
and update `tools/depmap/graph.json` for anything that moved. A session that
opens the whole architecture page, edits under `docs/`, or skips the map has
not read `CLAUDE.md`.
